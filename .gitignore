# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.local-voice/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code local config
.claude/

# Local environment (Supabase URL/keys); see .env.example
.env
.env.*
!.env.example

# Agent / MCP tooling (local, not part of the app)
.agents/
.mcp.json
skills-lock.json
graphify-out
design-qa-*.png

# Private rollout notes and store media are maintained outside the public app.
docs/cloud-sync-billing-rollout.md
# Drafting copies of the legal texts; the published ones live on the website.
legal/
assets/lemon-squeezy/

# Standalone private billing backend (publish only to a separate private repo).
/private/todofy-api/
